name: cli checks

on:
  push:
    branches: "main"

jobs:
  greeting:
    runs-on: ubuntu-latest
    container: 
      image: ghcr.io/devops-hemant/deployment:v0.0.1
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      issues: write
      pull-requests: write
    steps:
    - name: CLI checks
      run: |
        echo "checking Databricks cli version" && databricks --version 
        echo "Azure cli version" && az --version
        echo "dbsql version check" && dbsqlcli --version
        echo "yq version check" && yq --version
